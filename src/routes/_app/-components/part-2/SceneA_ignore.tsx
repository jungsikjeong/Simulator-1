import type { SceneKey } from "@/modules/scene-key.type";

export default function Part2_SceneA_ignore({ onSceneChange }: { onSceneChange: (scene: SceneKey) => void }) {


    return (
        <div>
        </div>
    )
}